# Firebase Configuration
# Get these values from your Firebase project settings:
# https://console.firebase.google.com/project/YOUR_PROJECT_ID/settings/general

# Required for all environments
VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com
VITE_OPENAI_API_KEY=sk-...your-key-here

# Development Notes:
# - For local development with emulators, these values can be dummy values
# - The app will automatically connect to Firebase Emulators in dev mode
# - For production deployment, use your actual Firebase project credentials

# Production Notes:
# - Never commit your actual .env file to version control
# - Add your production Firebase credentials when deploying to Vercel
# - Make sure to add your Vercel deployment URL to Firebase Auth authorized domains

# Emulator Configuration
# Set to 'true' to use Firebase Emulators (email/password auth only)
# Set to 'false' to use production Firebase (required for Google OAuth)
# Note: Google Sign-In does NOT work with Firebase Auth Emulator
VITE_USE_EMULATORS=true
